

const SubHeading = ({heading}: {heading: string}) => {
    return (
        <>
            <h2 className="uppercase text-white font-bold pb-2">{heading}</h2>
        </>
    );
};

export default SubHeading;